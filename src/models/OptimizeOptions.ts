/**
 * Options for optimizing a workbook.
 * @module OptimizeOptions
 * @category Models
 */

/**
 * Options for optimizing a workbook.
 */
export type OptimizeOptions = {
	compressionLevel?: number; // 1 to 9
};
