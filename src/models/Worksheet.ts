/**
 * Worksheet models.
 * @module Worksheet
 * @category Models
 */

import type { CellValue } from "aspose.cells.node";


export type WorksheetName = string & { readonly __brand: unique symbol };

// export type WorksheetIndex = number;
